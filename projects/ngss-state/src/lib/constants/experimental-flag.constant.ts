/**
 * Global experimental feature flags for @ngVault/core.
 *
 * Use these constants to control activation of opt-in APIs.
 * Set to `false` for production stability.
 */
export const NGVAULT_EXPERIMENTAL_HTTP_RESOURCE = true;
